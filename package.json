{
  "name": "rs-mcp-lunar-worker",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "worker-build --release",
    "dev": "npm run build && wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "check:startup": "wrangler check startup"
  },
  "devDependencies": {
    "wrangler": "4.112.0"
  }
}
